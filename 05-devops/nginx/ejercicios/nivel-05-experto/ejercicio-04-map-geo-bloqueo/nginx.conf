events { worker_connections 1024; }
http {
    # TODO: geo $blocked_country { default 0; <rangos> 1; }
    # TODO: map $blocked_country $access_action { 0 allow; 1 deny; }
    server {
        listen 8080;
        # TODO: root, index
        location / {
            # TODO: if ($access_action = deny) { return 403; }
            try_files $uri $uri/ =404;
        }
    }
}
