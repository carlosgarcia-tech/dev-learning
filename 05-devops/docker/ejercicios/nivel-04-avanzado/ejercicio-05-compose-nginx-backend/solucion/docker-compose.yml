services:
  proxy:
    image: nginx:1.27-alpine
    ports:
      - "8099:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on: [backend]
    networks: [proxynet]

  backend:
    build: ./app
    networks: [proxynet]

networks:
  proxynet:
