# TODO: crea tu Dockerfile multi-stage para el frontend
# Stage builder: FROM node:20-alpine AS builder, copia archivos
# Stage runtime: FROM nginx:1.27-alpine, copia estáticos a /usr/share/nginx/html
