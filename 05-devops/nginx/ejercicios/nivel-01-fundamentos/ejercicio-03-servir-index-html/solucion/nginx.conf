events { worker_connections 1024; }
http {
    server {
        listen 8080;
        server_name localhost;
        root /ruta/absoluta/a/web;
        index index.html;
        location / {
            try_files $uri $uri/ =404;
        }
    }
}
