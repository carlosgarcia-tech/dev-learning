# 02-secret.yaml — Secret con las contraseñas y claves de la app
# TODO: define un Secret "tienda-secrets" en el namespace "tienda-online".
apiVersion: v1
kind: Secret
metadata:
  name: tienda-secrets
  namespace: tienda-online
  labels:
    app: tienda
    component: config
type: Opaque
stringData:
  # TODO: añade estas claves (usa stringData para que sean legibles):
  #   POSTGRES_PASSWORD: "tienda-super-secreta"
  #   BACKEND_API_KEY: "api-key-tienda-2024"
  #   JWT_SECRET: "jwt-super-secreto-tienda"
  POSTGRES_PASSWORD: "tienda-super-secreta"
