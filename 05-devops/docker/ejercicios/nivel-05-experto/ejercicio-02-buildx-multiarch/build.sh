#!/usr/bin/env bash
# TODO: construye multi-arch con buildx
# docker buildx build --platform linux/amd64,linux/arm64 -t miuser/app:multiarch .
set -euo pipefail
echo "TODO: implementar buildx multi-arch"
