events { worker_connections 1024; }
http {
    server {
        listen 8080;
        # TODO: root e index
        # TODO: location = /old con return 301 /new
        # TODO: location = /new con return 200 "nueva ruta\n"
        # TODO: location /v1/ con rewrite ^/v1/(.*)$ /$1 break
        location / { try_files $uri $uri/ =404; }
    }
}
