events { worker_connections 1024; }
http {
    # TODO: upstream app_backend con least_conn y 2 servers
    server {
        listen 8080;
        location / {
            # TODO: proxy_pass y proxy_set_header Host y X-Real-IP
        }
    }
}
