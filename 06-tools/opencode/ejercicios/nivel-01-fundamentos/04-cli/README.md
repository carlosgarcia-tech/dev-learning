# 04 — CLI básico

## Enunciado

Usa el CLI de opencode.

## Requisitos

1. Explica en `respuesta.txt` la diferencia entre `opencode` (interactivo) y `opencode run "prompt"` (headless).
2. Menciona cuándo usar cada uno.

## Solución

<details>
<summary>Mostrar solución</summary>

`respuesta.txt`:
```
opencode: abre la TUI interactiva para desarrollo.
opencode run "prompt": ejecuta en modo headless sin interfaz, ideal para scripts y CI/CD.
```

</details>
