# TODO: crea tu Dockerfile multi-stage para el backend
# Stage builder: FROM node:20-alpine AS builder, COPY package*.json, npm ci, COPY src/
# Stage runtime: FROM node:20-alpine, addgroup/adduser, COPY --from=builder node_modules y src, USER app, HEALTHCHECK, CMD ["node","src/server.js"]
