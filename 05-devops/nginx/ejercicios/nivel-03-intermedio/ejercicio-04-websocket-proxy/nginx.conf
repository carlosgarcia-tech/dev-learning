events { worker_connections 1024; }
http {
    # TODO: map $http_upgrade $connection_upgrade { default upgrade; '' close; }
    # TODO: upstream ws_backend con server 127.0.0.1:9001
    server {
        listen 8080;
        location /ws {
            # TODO: proxy_pass http://ws_backend;
            # TODO: proxy_http_version 1.1;
            # TODO: proxy_set_header Upgrade $http_upgrade;
            # TODO: proxy_set_header Connection $connection_upgrade;
            # TODO: proxy_read_timeout 3600s;
        }
    }
}
