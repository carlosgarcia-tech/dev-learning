# TODO: crea tu docker-compose.yml
# services:
#   proxy: nginx:1.27-alpine, ports 8099:80, volume ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro, depends_on [backend], networks [proxynet]
#   backend: build ./app, networks [proxynet]
# networks: proxynet
