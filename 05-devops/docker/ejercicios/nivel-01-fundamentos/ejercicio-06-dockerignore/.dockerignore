# TODO: crea tu .dockerignore completo y tu Dockerfile
# .dockerignore debe excluir: node_modules, .git, *.log, secrets.env, .env, Dockerfile*, solucion/
# Dockerfile: FROM node:20-alpine, WORKDIR /app, COPY app/package.json, npm ci --omit=dev, COPY app/, EXPOSE 3000, CMD ["node","server.js"]
