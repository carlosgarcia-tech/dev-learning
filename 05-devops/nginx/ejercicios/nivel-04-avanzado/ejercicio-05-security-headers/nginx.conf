events { worker_connections 1024; }
http {
    server {
        listen 8080;
        # TODO: root e index
        # TODO: add_header Strict-Transport-Security "max-age=31536000" always
        # TODO: add_header X-Frame-Options "SAMEORIGIN" always
        # TODO: add_header X-Content-Type-Options "nosniff" always
        # TODO: add_header Referrer-Policy "strict-origin-when-cross-origin" always
        location / { try_files $uri $uri/ =404; }
    }
}
