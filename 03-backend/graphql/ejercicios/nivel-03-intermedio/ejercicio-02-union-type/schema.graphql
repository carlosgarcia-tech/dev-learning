type User {
  id: ID!
  name: String!
  email: String
}

type Post {
  id: ID!
  title: String!
  body: String!
}

type Comment {
  id: ID!
  text: String!
}

union SearchResult = User | Post | Comment

type Query {
  search(term: String!): [SearchResult!]!
}
