// Service worker aquí
const CACHE = 'mi-pwa-v1';

self.addEventListener('install', (e) => {
  // TODO: abrir cache y pre-cachear recursos
});

self.addEventListener('fetch', (e) => {
  // TODO: responder desde cache o red
});
