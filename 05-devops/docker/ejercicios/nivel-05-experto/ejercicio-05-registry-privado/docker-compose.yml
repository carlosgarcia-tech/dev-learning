# TODO: crea tu docker-compose.yml
# services:
#   registry:
#     image: registry:2
#     ports: ["5000:5000"]
#     volumes: [registry_data:/var/lib/registry]
#     restart: unless-stopped
# volumes:
#   registry_data:
