# Ejercicio 02 — Servidor HTTP

- **Nivel:** 5/5
- **Tema:** Servidor HTTP con tipos
- **Tiempo estimado:** 20 minutos

## Enunciado

Completa `index.ts` implementando los conceptos de **Servidor HTTP** (Servidor HTTP con tipos),
siguiendo el estilo mostrado en las guías de este curso (ver
`../../../05-*.md` o la guía correspondiente en la raíz de
`typescript/`).

## Requisitos

- [ ] El archivo compila sin errores (`npx tsc --noEmit index.ts`)
- [ ] Se implementan todas las funciones marcadas con `TODO` en `index.ts`
- [ ] No se usa `any` salvo que el enunciado lo permita explícitamente
- [ ] Los tests pasan: `node --test index.test.ts`

## Pistas

<details>
<summary>Mostrar pistas</summary>

Revisa la guía correspondiente a este nivel en `es/01-programming/typescript/`
y los ejemplos de código de la sección relacionada con **Servidor HTTP**.

</details>

## Solución

<details>
<summary>Mostrar solución</summary>

La solución de referencia se entrega junto con cada ejercicio implementado en
clase / repositorio del curso. Compara tu implementación de `index.ts` verificando
que pase todos los tests de `index.test.ts`.

</details>
