worker_processes auto;
worker_rlimit_nofile 65535;

events {
    worker_connections 1024;
}

http {
    # TODO: subir worker_connections a 4096, multi_accept on
    # TODO: server_tokens off, gzip, log_format con request_time
    # TODO: limit_req_zone, proxy_cache_path
    # TODO: upstream backend con 2 servers max_fails=3 fail_timeout=30s + keepalive 32
    # TODO: server listen 80 con return 301 https
    # TODO: server listen 443 ssl http2 con TLS1.2/1.3, security headers, /api/ con proxy_cache+limit_req, / con expires
    server {
        listen 8080;
        location / { return 200 "TODO\n"; }
    }
}
