events { worker_connections 1024; }
http {
    # TODO: upstream app_backend con 2 servers, max_fails=3 fail_timeout=30s cada uno
    server {
        listen 8080;
        location / {
            # TODO: proxy_pass http://app_backend;
            # TODO: proxy_next_upstream error timeout http_502 http_503 http_504;
            # TODO: proxy_next_upstream_tries 2;
        }
    }
}
