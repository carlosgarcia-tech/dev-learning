apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: app
data:
  LOG_LEVEL: "info"
  ENTORNO: "desarrollo"
  config.yaml: |
    servidor:
      puerto: 8080
      host: 0.0.0.0
    debug: false
