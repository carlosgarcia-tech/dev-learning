# TODO: crea tu docker-compose.dev.yml (override de desarrollo)
# services:
#   app:
#     environment:
#       NODE_ENV: development
#     volumes:
#       - ./app/src:/app/src
#     command: ["node", "--watch", "src/server.js"]
