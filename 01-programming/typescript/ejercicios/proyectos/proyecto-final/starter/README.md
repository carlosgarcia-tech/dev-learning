# tasks-api (starter)

Esqueleto inicial para el proyecto final. Instala dependencias y completa cada
módulo siguiendo el README principal del proyecto (`../README.md`).

```bash
npm install
cp .env.example .env
npm run dev
```
