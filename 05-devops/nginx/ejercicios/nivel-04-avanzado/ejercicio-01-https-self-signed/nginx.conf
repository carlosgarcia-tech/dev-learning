events { worker_connections 1024; }
http {
    server {
        listen 443 ssl;
        server_name localhost;
        # TODO: ssl_certificate, ssl_certificate_key, ssl_protocols
        # TODO: root, index, location /
    }
}
