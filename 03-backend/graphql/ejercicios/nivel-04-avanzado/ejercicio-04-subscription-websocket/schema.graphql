type Post {
  id: ID!
  title: String!
  body: String!
}

input CreatePostInput {
  title: String!
  body: String!
}

type Query {
  post(id: ID!): Post
}

type Mutation {
  createPost(input: CreatePostInput!): Post!
}

type Subscription {
  postAdded: Post!
}
